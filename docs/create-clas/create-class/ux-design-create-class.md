# UX Design: Create Class Workflow

**Feature:** Tạo Lớp Học Mới (7-Step Workflow)
**Target Role:** ACADEMIC_AFFAIR
**Design System:** Modern Minimal (shadcn/ui + Tailwind CSS)
**Last Updated:** November 9, 2025

---

## 📋 Mục Lục

1. [Tổng Quan UX](#1-tổng-quan-ux)
2. [Design Principles](#2-design-principles)
3. [User Journey Map](#3-user-journey-map)
4. [Wireframes Chi Tiết](#4-wireframes-chi-tiết-7-steps)
5. [Component Specifications](#5-component-specifications)
6. [Interactive States](#6-interactive-states)
7. [Error Handling UX](#7-error-handling-ux)
8. [Responsive Design](#8-responsive-design)
9. [Accessibility](#9-accessibility)
10. [Implementation Notes](#10-implementation-notes)

---

## 1. Tổng Quan UX

### 1.1 Mục Tiêu Trải Nghiệm

**Primary Goal:** Giúp Academic Affairs staff tạo lớp học mới một cách nhanh chóng, chính xác và không gây rối loạn.

**UX Objectives:**
- ✅ **Clarity**: User luôn biết mình đang ở bước nào trong workflow
- ✅ **Guidance**: Hướng dẫn rõ ràng cho mỗi bước, tránh confusion
- ✅ **Error Prevention**: Validate inputs trước khi submit, conflict detection
- ✅ **Efficiency**: Minimize số lần click, support keyboard navigation
- ✅ **Transparency**: Hiển thị loading states, processing time, success/error feedback
- ✅ **Flexibility**: Cho phép quay lại bước trước, edit assignments

### 1.2 Key User Flows

```
Entry Point → [Step 1-7] → Success State
              ↓ (có conflict)
         Conflict Resolution → Retry → Success
              ↓ (có lỗi)
         Error State → Fix → Retry → Success
```

### 1.3 Screen Layout Architecture

```
┌─────────────────────────────────────────────────────────┐
│ Header: "Tạo Lớp Học Mới"                    [Lưu nháp] │
├─────────────────────────────────────────────────────────┤
│ Progress Indicator: [1] [2] [3] [4] [5] [6] [7]        │
│                     ●────────────────────────           │
├─────────────────────────────────────────────────────────┤
│                                                         │
│         Step Content Area (Dynamic)                    │
│         - Form fields / Data tables                    │
│         - Validation messages                          │
│         - Help text                                    │
│                                                         │
├─────────────────────────────────────────────────────────┤
│ Footer Actions:                                         │
│ [← Quay lại]              [Bỏ qua]      [Tiếp theo →] │
└─────────────────────────────────────────────────────────┘
```

---

## 2. Design Principles

### 2.1 Modern Minimal Aesthetics

Theo design system hiện tại ([docs/uiux-design.md](../../../uiux-design.md)):

- **Neutral base colors**: Trắng/xám làm nền chính
- **Subtle accent**: Màu tím (hoặc brand color) chỉ cho primary actions
- **Generous whitespace**: 8px grid system, không lấp đầy mọi pixel
- **System fonts**: Inter/SF Pro với font hierarchy rõ ràng
- **Minimal animations**: Subtle, purposeful motion (không overuse)
- **High contrast**: WCAG AA compliance

### 2.2 Content-First Design

- **Tránh overuse Card components**: Chỉ dùng Card khi cần group content logic
- **Direct presentation**: Ưu tiên hiển thị trực tiếp thay vì wrap trong containers
- **Vietnamese 100%**: Tất cả UI text phải bằng tiếng Việt
- **Purposeful padding**: Sử dụng padding có mục đích, tránh excessive whitespace gây disconnect

### 2.3 User-Centric Patterns

- **Progressive disclosure**: Chỉ hiển thị thông tin cần thiết cho mỗi step
- **Inline validation**: Validate ngay khi user blur khỏi field
- **Smart defaults**: Pre-fill fields khi có thể (e.g., default modality, common time slots)
- **Undo/Redo**: Cho phép quay lại bước trước mà không mất data

---

## 3. User Journey Map

### 3.1 Happy Path (No Conflicts)

```
Start
  ↓
[STEP 1] Nhập thông tin lớp học cơ bản
  ↓ (200 OK, classId: 123)
[STEP 2] Backend auto-generate 36 sessions
  ↓ (optional: review sessions)
[STEP 3] Gán time slots theo pattern ngày
  ↓ (36/36 success)
[STEP 4] Gán resources (phòng học/online)
  ↓ (36/36 success, conflictCount: 0)
[STEP 5A] Xem danh sách teachers available
  ↓ (select teacher với 100% availability)
[STEP 5B] Gán teacher cho tất cả sessions
  ↓ (36/36 success, needsSubstitute: false)
[STEP 6] Validate class completeness
  ↓ (isValid: true)
[STEP 7] Submit for approval
  ↓ (status: SCHEDULED, approvalStatus: PENDING)
Success! 🎉
```

### 3.2 Conflict Path (Có Conflicts)

```
[STEP 4] Gán resources
  ↓ (32/36 success, conflictCount: 4)
⚠️ Conflict Dialog hiển thị 4 sessions
  ↓
User chọn: "Giải quyết thủ công"
  ↓
Conflict Resolution UI:
  - Table hiển thị 4 sessions có conflict
  - Dropdown chọn resource khác cho từng session
  - "Thử lại" button
  ↓ (retry)
  ↓ (36/36 success)
Continue to STEP 5
```

### 3.3 Validation Error Path

```
[STEP 1] User submit form với data invalid
  ↓
Frontend validation errors hiển thị inline
  ↓
User fix errors
  ↓
Retry submit
  ↓ (400 Bad Request - backend validation)
Backend validation errors hiển thị
  ↓
User fix
  ↓
Retry → Success
```

---

## 4. Wireframes Chi Tiết (7 Steps)

### STEP 1: Tạo Lớp Học Cơ Bản

**Layout: Form-based với 2 columns**

```
┌──────────────────────────────────────────────────────────┐
│ Bước 1: Thông Tin Lớp Học                               │
│ Nhập thông tin cơ bản để tạo lớp học mới                │
├──────────────────────────────────────────────────────────┤
│                                                          │
│ ┌─────────────────┬─────────────────┐                   │
│ │ Mã Lớp *        │ Tên Lớp *       │                   │
│ │ [CS101-A-2025]  │ [Computer...]   │                   │
│ └─────────────────┴─────────────────┘                   │
│                                                          │
│ ┌─────────────────┬─────────────────┐                   │
│ │ Chi Nhánh *     │ Khóa Học *      │                   │
│ │ [Dropdown ▼]    │ [Dropdown ▼]    │                   │
│ └─────────────────┴─────────────────┘                   │
│                                                          │
│ ┌─────────────────┬─────────────────┐                   │
│ │ Hình Thức *     │ Sức Chứa *      │                   │
│ │ [○ Online       │ [30]            │                   │
│ │  ● Offline      │                 │                   │
│ │  ○ Hybrid]      │                 │                   │
│ └─────────────────┴─────────────────┘                   │
│                                                          │
│ ┌─────────────────────────────────────────┐             │
│ │ Ngày Bắt Đầu *                           │             │
│ │ [📅 15/01/2025]                          │             │
│ └─────────────────────────────────────────┘             │
│                                                          │
│ ┌─────────────────────────────────────────┐             │
│ │ Lịch Học * (Chọn các ngày trong tuần)    │             │
│ │ [☐ CN] [☑ T2] [☐ T3] [☑ T4]             │             │
│ │ [☐ T5] [☑ T6] [☐ T7]                    │             │
│ └─────────────────────────────────────────┘             │
│                                                          │
│ ℹ️ Hệ thống sẽ tự động tạo 36 buổi học dựa trên         │
│    lịch học và thời lượng khóa học                      │
│                                                          │
├──────────────────────────────────────────────────────────┤
│                    [Hủy]        [Tạo Lớp Học →]         │
└──────────────────────────────────────────────────────────┘
```

**Component Mapping:**
- **Field groups**: `<Field>` compound component
- **Radio group**: `<ToggleGroup>` for modality
- **Date picker**: `<Calendar>` + `<Popover>`
- **Checkbox group**: `<Checkbox>` for schedule days
- **Buttons**: `<Button variant="outline">` (Hủy) + `<Button>` (Primary)

**Validation Rules:**
- Mã lớp: Required, max 50 chars, uppercase/numbers/hyphens only, unique
- Tên lớp: Required, max 255 chars
- Chi nhánh: Required, must exist
- Khóa học: Required, must exist
- Hình thức: Required, one of ONLINE/OFFLINE/HYBRID
- Ngày bắt đầu: Required, must be future date
- Lịch học: Required, 1-7 days selected
- Sức chứa: Required, 1-1000

**Success State:**
- POST `/api/v1/classes` returns 200 OK
- Toast notification: "Lớp CS101-A-2025 đã được tạo với 36 buổi học"
- Auto-navigate to STEP 3 (skip STEP 2 - backend handles session generation)

---

### STEP 2: Review Sessions (Optional)

**Layout: Table view với filter/search**

```
┌──────────────────────────────────────────────────────────┐
│ Bước 2: Xem Lại Buổi Học (Tùy chọn)                     │
│ 36 buổi học đã được tạo tự động                         │
├──────────────────────────────────────────────────────────┤
│                                                          │
│ 📊 Tổng quan                                             │
│ • Tổng số buổi: 36 buổi                                 │
│ • Từ ngày: 15/01/2025 → Đến ngày: 15/05/2025           │
│ • Các ngày: Thứ 2, Thứ 4, Thứ 6                        │
│                                                          │
│ ┌────────────────────────────────────────────────────┐  │
│ │ # │ Ngày        │ Thứ  │ Buổi │ Trạng thái      │  │
│ ├───┼─────────────┼──────┼──────┼─────────────────┤  │
│ │ 1 │ 15/01/2025  │ T2   │ -    │ Chưa gán        │  │
│ │ 2 │ 17/01/2025  │ T4   │ -    │ Chưa gán        │  │
│ │ 3 │ 19/01/2025  │ T6   │ -    │ Chưa gán        │  │
│ │...│ ...         │ ...  │ ...  │ ...             │  │
│ │36 │ 15/05/2025  │ T6   │ -    │ Chưa gán        │  │
│ └────────────────────────────────────────────────────┘  │
│                                                          │
│ ℹ️ Bạn có thể tiếp tục để gán thời gian và tài nguyên   │
│                                                          │
├──────────────────────────────────────────────────────────┤
│ [← Quay lại]                        [Tiếp theo →]       │
└──────────────────────────────────────────────────────────┘
```

**Component Mapping:**
- **Table**: `<Table>` component
- **Info card**: Simple div với icon, không dùng `<Card>` (theo principle: avoid overuse cards)

**Interactions:**
- **Optional skip**: User có thể bỏ qua bước này và đi thẳng STEP 3
- **Read-only**: Không cho edit sessions ở đây

---

### STEP 3: Gán Time Slots

**Layout: Pattern-based assignment với visual day selector**

```
┌──────────────────────────────────────────────────────────┐
│ Bước 3: Gán Khung Giờ Học                               │
│ Chọn khung giờ cho từng ngày trong tuần                 │
├──────────────────────────────────────────────────────────┤
│                                                          │
│ Gán theo pattern (Khuyên dùng)                          │
│                                                          │
│ ┌────────────────────────────────────────────────────┐  │
│ │ Thứ 2          [📅 Buổi sáng (8:00-10:00) ▼]     │  │
│ │ → 12 buổi học                                      │  │
│ ├────────────────────────────────────────────────────┤  │
│ │ Thứ 4          [📅 Buổi sáng (8:00-10:00) ▼]     │  │
│ │ → 12 buổi học                                      │  │
│ ├────────────────────────────────────────────────────┤  │
│ │ Thứ 6          [📅 Buổi chiều (14:00-16:00) ▼]   │  │
│ │ → 12 buổi học                                      │  │
│ └────────────────────────────────────────────────────┘  │
│                                                          │
│ 💡 Tất cả buổi học trong cùng ngày sẽ có cùng khung giờ │
│                                                          │
│ ┌────────────────────────────────────────────────────┐  │
│ │ Preview                                            │  │
│ │ • Thứ 2: 12 buổi × Buổi sáng (8:00-10:00)        │  │
│ │ • Thứ 4: 12 buổi × Buổi sáng (8:00-10:00)        │  │
│ │ • Thứ 6: 12 buổi × Buổi chiều (14:00-16:00)      │  │
│ │ ─────────────────────────────────────────────────  │  │
│ │ Tổng: 36/36 buổi đã gán                           │  │
│ └────────────────────────────────────────────────────┘  │
│                                                          │
├──────────────────────────────────────────────────────────┤
│ [← Quay lại]                  [Gán Khung Giờ →]         │
└──────────────────────────────────────────────────────────┘
```

**Component Mapping:**
- **Day + Time slot pairs**: Custom component với `<Select>` dropdown
- **Preview box**: Simple bordered div (không dùng Card)
- **Progress indicator**: Text display "36/36 buổi đã gán"

**API Call:**
```typescript
POST /api/v1/classes/{classId}/time-slots
{
  "assignments": [
    { "dayOfWeek": 1, "timeSlotTemplateId": 5 },
    { "dayOfWeek": 3, "timeSlotTemplateId": 5 },
    { "dayOfWeek": 5, "timeSlotTemplateId": 6 }
  ]
}
```

**Success State:**
- Response: `{ successCount: 36, failedCount: 0 }`
- Toast: "Đã gán khung giờ cho 36/36 buổi học"
- Auto-navigate to STEP 4

---

### STEP 4: Gán Resources (Phòng Học/Online)

**Layout: Similar to STEP 3, pattern-based với conflict handling**

```
┌──────────────────────────────────────────────────────────┐
│ Bước 4: Gán Tài Nguyên (Phòng Học/Online)              │
│ Chọn phòng học hoặc tài khoản online cho từng ngày     │
├──────────────────────────────────────────────────────────┤
│                                                          │
│ Gán theo pattern                                         │
│                                                          │
│ ┌────────────────────────────────────────────────────┐  │
│ │ Thứ 2          [🏢 Phòng A101 (30 chỗ) ▼]        │  │
│ │ → 12 buổi học                                      │  │
│ ├────────────────────────────────────────────────────┤  │
│ │ Thứ 4          [🏢 Phòng A101 (30 chỗ) ▼]        │  │
│ │ → 12 buổi học                                      │  │
│ ├────────────────────────────────────────────────────┤  │
│ │ Thứ 6          [🏢 Phòng A102 (40 chỗ) ▼]        │  │
│ │ → 12 buổi học                                      │  │
│ └────────────────────────────────────────────────────┘  │
│                                                          │
│ [🔍 Kiểm tra xung đột]                                   │
│                                                          │
├──────────────────────────────────────────────────────────┤
│ [← Quay lại]                  [Gán Tài Nguyên →]        │
└──────────────────────────────────────────────────────────┘
```

**API Call:**
```typescript
POST /api/v1/classes/{classId}/resources
{
  "pattern": [
    { "dayOfWeek": 1, "resourceId": 101 },
    { "dayOfWeek": 3, "resourceId": 101 },
    { "dayOfWeek": 5, "resourceId": 102 }
  ]
}
```

**Success State (No Conflicts):**
- Response: `{ successCount: 36, conflictCount: 0, processingTimeMs: 142 }`
- Toast: "Đã gán tài nguyên cho 36/36 buổi học (142ms)"
- Auto-navigate to STEP 5A

**Conflict State:**
- Response: `{ successCount: 32, conflictCount: 4, conflicts: [...] }`
- **Show Conflict Dialog** (see [Conflict Resolution Dialog](#conflict-resolution-dialog))

---

### STEP 5A: Xem Teachers Available (PRE-CHECK)

**Layout: Table với sorting, filtering, availability indicators**

```
┌──────────────────────────────────────────────────────────┐
│ Bước 5A: Chọn Giáo Viên                                 │
│ Xem danh sách giáo viên có thể dạy lớp này              │
├──────────────────────────────────────────────────────────┤
│                                                          │
│ [🔍 Tìm kiếm...]   [Lọc: ▼ Tất cả]   [Sắp xếp: ▼ %↓]  │
│                                                          │
│ ┌────────────────────────────────────────────────────┐  │
│ │ Giáo viên        │Skills│Kinh nghiệm│Khả dụng│Chọn│  │
│ ├──────────────────┼──────┼───────────┼────────┼────┤  │
│ │ ✅ John Smith    │JAVA  │ 5 năm     │ 100%  │[●]│  │
│ │   john@tms.edu   │SPRING│           │36/36   │    │  │
│ │                  │      │           │        │    │  │
│ ├──────────────────┼──────┼───────────┼────────┼────┤  │
│ │ ⚠️ Jane Doe      │GENERA│ 8 năm     │ 88.9% │[○]│  │
│ │   jane@tms.edu   │      │           │32/36   │    │  │
│ │   [Xem 4 xung đột]                             │    │  │
│ │                  │      │           │        │    │  │
│ ├──────────────────┼──────┼───────────┼────────┼────┤  │
│ │ ❌ Bob Lee       │PYTHON│ 3 năm     │ 0%    │[-]│  │
│ │   bob@tms.edu    │      │           │0/36    │    │  │
│ └────────────────────────────────────────────────────┘  │
│                                                          │
│ 💡 Giáo viên có ✅ xanh sẵn sàng cho tất cả buổi học   │
│                                                          │
├──────────────────────────────────────────────────────────┤
│ [← Quay lại]                  [Tiếp theo →]             │
└──────────────────────────────────────────────────────────┘
```

**API Call:**
```typescript
GET /api/v1/classes/{classId}/available-teachers
```

**Component Mapping:**
- **Table**: `<Table>` component
- **Availability badges**:
  - ✅ `<Badge variant="success">` - FULLY_AVAILABLE (green)
  - ⚠️ `<Badge variant="warning">` - PARTIALLY_AVAILABLE (yellow)
  - ❌ `<Badge variant="destructive">` - UNAVAILABLE (red)
- **Radio selection**: `<RadioGroup>` for teacher selection
- **Expand conflicts**: `<Collapsible>` row for conflict details

**Interactions:**
- **Sort by availability %**: Default descending (100% → 0%)
- **Click "Xem 4 xung đột"**: Expand row hiển thị conflicting sessions table
- **Select teacher**: Radio button → enable "Tiếp theo"
- **Tooltip on hover**: Skills, experience details

---

### STEP 5B: Gán Teacher

**Layout: Confirmation với option full/partial assignment**

```
┌──────────────────────────────────────────────────────────┐
│ Bước 5B: Gán Giáo Viên                                  │
│ Xác nhận gán giáo viên cho các buổi học                 │
├──────────────────────────────────────────────────────────┤
│                                                          │
│ Giáo viên đã chọn:                                       │
│ ┌────────────────────────────────────────────────────┐  │
│ │ 👨‍🏫 John Smith                                       │  │
│ │    john.smith@tms.edu                               │  │
│ │    Skills: JAVA, SPRING_BOOT                        │  │
│ │    Kinh nghiệm: 5 năm                               │  │
│ │    Khả dụng: 36/36 buổi (100%) ✅                   │  │
│ └────────────────────────────────────────────────────┘  │
│                                                          │
│ Phạm vi gán:                                             │
│ ● Gán cho tất cả buổi học (36 buổi)                    │
│ ○ Gán cho các buổi cụ thể                              │
│                                                          │
│ ℹ️ Giáo viên này có thể dạy tất cả buổi học             │
│                                                          │
├──────────────────────────────────────────────────────────┤
│ [← Quay lại]                  [Gán Giáo Viên →]         │
└──────────────────────────────────────────────────────────┘
```

**API Call (Full Assignment):**
```typescript
POST /api/v1/classes/{classId}/teachers
{
  "teacherId": 101
}
```

**Success State:**
- Response: `{ assignedCount: 36, needsSubstitute: false }`
- Toast: "Đã gán giáo viên cho 36/36 buổi học (42ms)"
- Auto-navigate to STEP 6

**Partial Assignment State:**
- Response: `{ assignedCount: 12, needsSubstitute: true, remainingSessions: 24 }`
- Show dialog: "24 buổi học chưa có giáo viên. Bạn muốn gán giáo viên khác không?"
- Options: [Gán giáo viên khác] [Tiếp tục]

---

### STEP 6: Validate Class

**Layout: Checklist-style validation summary**

```
┌──────────────────────────────────────────────────────────┐
│ Bước 6: Kiểm Tra Đầy Đủ                                │
│ Đảm bảo tất cả thông tin đã được gán đầy đủ            │
├──────────────────────────────────────────────────────────┤
│                                                          │
│ Đang kiểm tra... ⏳                                      │
│                                                          │
│ ✅ Khung giờ học: 36/36 buổi                            │
│ ✅ Tài nguyên: 36/36 buổi                               │
│ ✅ Giáo viên: 36/36 buổi                                │
│                                                          │
│ ┌────────────────────────────────────────────────────┐  │
│ │ 🎉 Lớp học đã sẵn sàng để gửi duyệt!               │  │
│ │                                                     │  │
│ │ Tổng quan:                                          │  │
│ │ • Mã lớp: CS101-A-2025                             │  │
│ │ • Tên lớp: Computer Science 101                    │  │
│ │ • Số buổi: 36 buổi                                 │  │
│ │ • Giáo viên: John Smith                            │  │
│ │ • Phòng học: A101 (T2, T4), A102 (T6)             │  │
│ └────────────────────────────────────────────────────┘  │
│                                                          │
├──────────────────────────────────────────────────────────┤
│ [← Quay lại]                  [Gửi Duyệt →]             │
└──────────────────────────────────────────────────────────┘
```

**API Call:**
```typescript
POST /api/v1/classes/{classId}/validate
```

**Success State (Valid):**
- Response: `{ isValid: true, validationSummary: {...} }`
- Show summary với checkmarks
- Enable "Gửi Duyệt" button

**Error State (Invalid):**
- Response: `{ isValid: false, missingAssignments: [...] }`
- Show error list:
  ```
  ❌ Thiếu tài nguyên: 4 buổi
  • Buổi #5 (15/01/2025)
  • Buổi #12 (29/01/2025)
  • ...

  [Quay lại Bước 4]
  ```

---

### STEP 7: Submit for Approval

**Layout: Final confirmation với summary**

```
┌──────────────────────────────────────────────────────────┐
│ Bước 7: Gửi Duyệt                                       │
│ Xác nhận và gửi lớp học cho Trưởng Chi Nhánh duyệt     │
├──────────────────────────────────────────────────────────┤
│                                                          │
│ Thông tin lớp học:                                       │
│                                                          │
│ Mã lớp:         CS101-A-2025                            │
│ Tên lớp:        Computer Science 101                    │
│ Chi nhánh:      Cơ sở Hà Nội                            │
│ Khóa học:       Lập trình Java Cơ bản                   │
│ Hình thức:      Offline                                 │
│ Ngày bắt đầu:   15/01/2025                              │
│ Ngày kết thúc:  15/05/2025 (dự kiến)                   │
│ Số buổi:        36 buổi                                 │
│ Lịch học:       Thứ 2, Thứ 4, Thứ 6                    │
│ Sức chứa:       30 học viên                             │
│ Giáo viên:      John Smith                              │
│ Phòng học:      A101 (T2, T4), A102 (T6)               │
│                                                          │
│ ⚠️ Sau khi gửi, lớp học sẽ ở trạng thái "Chờ duyệt"    │
│    và bạn không thể chỉnh sửa cho đến khi được duyệt   │
│    hoặc bị từ chối.                                     │
│                                                          │
│ [☑] Tôi xác nhận thông tin trên là chính xác            │
│                                                          │
├──────────────────────────────────────────────────────────┤
│ [← Quay lại]      [Lưu nháp]      [Gửi Duyệt →]        │
└──────────────────────────────────────────────────────────┘
```

**API Call:**
```typescript
POST /api/v1/classes/{classId}/submit
```

**Success State:**
- Response: `{ status: "SCHEDULED", approvalStatus: "PENDING" }`
- Show success dialog:
  ```
  ┌──────────────────────────────────────────┐
  │ ✅ Lớp học đã được gửi duyệt thành công  │
  │                                          │
  │ Mã lớp: CS101-A-2025                    │
  │ Trạng thái: Chờ duyệt                   │
  │ Người duyệt: Trưởng Chi Nhánh           │
  │                                          │
  │ Bạn sẽ nhận được thông báo khi lớp học │
  │ được duyệt hoặc bị từ chối.             │
  │                                          │
  │        [Xem lớp học] [Về trang chủ]     │
  └──────────────────────────────────────────┘
  ```

---

## 5. Component Specifications

### 5.1 Progress Indicator Component

**Visual Design:**
```
Step 1      Step 2      Step 3      Step 4      Step 5      Step 6      Step 7
  ●────────────○────────────○────────────○────────────○────────────○────────────○
Active    Incomplete  Incomplete  Incomplete  Incomplete  Incomplete  Incomplete

After completing Step 1:
  ✓────────────●────────────○────────────○────────────○────────────○────────────○
Complete    Active    Incomplete  ...
```

**States:**
- ✓ **Complete**: Green circle với checkmark
- ● **Active**: Purple/brand color circle (larger)
- ○ **Incomplete**: Gray outline circle
- **Line**: Connects circles, green (complete), gray (incomplete)

**Component Props:**
```typescript
interface ProgressIndicatorProps {
  currentStep: 1 | 2 | 3 | 4 | 5 | 6 | 7
  completedSteps: number[]
  onStepClick?: (step: number) => void  // Allow navigate back
}
```

**Component Mapping:**
- Use `<Tabs>` component as base
- Custom styling với Tailwind

---

### 5.2 Conflict Resolution Dialog

**Layout: Modal với table + action buttons**

```
┌──────────────────────────────────────────────────────────┐
│ ⚠️ Phát Hiện Xung Đột Tài Nguyên              [✕ Đóng] │
├──────────────────────────────────────────────────────────┤
│                                                          │
│ Đã gán thành công: 32/36 buổi                          │
│ Xung đột: 4 buổi cần giải quyết thủ công               │
│                                                          │
│ Chi tiết xung đột:                                       │
│                                                          │
│ ┌────────────────────────────────────────────────────┐  │
│ │#│Ngày      │Phòng  │Lý do          │Giải pháp   │  │
│ ├─┼──────────┼───────┼───────────────┼────────────┤  │
│ │5│15/01/2025│A101   │Vượt sức chứa  │[▼ Chọn]   │  │
│ │ │          │       │(30 > 25)      │            │  │
│ │ │          │       │Xung đột: CS102│            │  │
│ ├─┼──────────┼───────┼───────────────┼────────────┤  │
│ │8│22/01/2025│A101   │Đã được đặt    │[▼ Chọn]   │  │
│ │ │          │       │Xung đột: CS103│            │  │
│ ├─┼──────────┼───────┼───────────────┼────────────┤  │
│ │...                                              │  │
│ └────────────────────────────────────────────────────┘  │
│                                                          │
│ Thời gian xử lý: 158ms                                  │
│                                                          │
├──────────────────────────────────────────────────────────┤
│        [Bỏ qua]            [Giải quyết & Thử lại]       │
└──────────────────────────────────────────────────────────┘
```

**Component Mapping:**
- **Modal**: `<Dialog>` component (full screen on mobile)
- **Table**: `<Table>` component
- **Dropdown**: `<Select>` for alternative resources
- **Buttons**: `<Button variant="outline">` (Bỏ qua) + `<Button>` (Giải quyết)

**Interactions:**
1. User clicks "Giải quyết & Thử lại"
2. Frontend calls individual session assignment APIs for 4 sessions
3. Retry bulk assignment for remaining sessions
4. If success → close dialog, continue to STEP 5
5. If still conflicts → update dialog với new conflict list

---

### 5.3 Form Field Component

**Standard Field Layout:**
```
┌─────────────────────────────────┐
│ Label *                         │  ← Label with required indicator
│ [Input field]                   │  ← Input/Select/Textarea
│ ℹ️ Help text                     │  ← Optional help text
│ ❌ Error message                 │  ← Validation error (conditional)
└─────────────────────────────────┘
```

**Component Props:**
```typescript
interface FieldProps {
  label: string
  required?: boolean
  helpText?: string
  error?: string
  children: React.ReactNode  // Input component
}
```

**Component Mapping:**
- Use existing `<Field>`, `<FieldLabel>`, `<FieldGroup>` compound components
- Error state: `<Alert variant="destructive">` hoặc inline text với icon

---

### 5.4 Action Buttons Layout

**Footer Fixed Position:**
```
┌──────────────────────────────────────────────────────────┐
│                     Content Area                         │
│                     (scrollable)                         │
└──────────────────────────────────────────────────────────┘
┌──────────────────────────────────────────────────────────┐
│ [← Quay lại]              [Lưu nháp]      [Tiếp theo →] │  ← Fixed footer
└──────────────────────────────────────────────────────────┘
```

**Button Variants:**
- **Quay lại**: `<Button variant="ghost">` với icon ChevronLeft
- **Lưu nháp**: `<Button variant="outline">`
- **Tiếp theo/Submit**: `<Button>` (primary) với icon ChevronRight

**Loading State:**
```tsx
<Button disabled>
  <Loader2 className="animate-spin" />
  Đang xử lý...
</Button>
```

---

## 6. Interactive States

### 6.1 Loading States

**Global Loading (during API calls):**
```
┌──────────────────────────────────────────┐
│                                          │
│           ⏳ Đang xử lý...               │
│   Đang gán tài nguyên cho 36 buổi học   │
│                                          │
│     [━━━━━━━━━━━━━━━━━━━━] 75%         │
│                                          │
└──────────────────────────────────────────┘
```

**Skeleton Loading (for tables):**
- Use `<Skeleton>` component for table rows while fetching data

**Inline Loading (button states):**
```tsx
// Before submit
<Button>Tạo Lớp Học →</Button>

// During submit
<Button disabled>
  <Loader2 className="animate-spin mr-2" />
  Đang tạo...
</Button>

// After success
<Button disabled>
  <Check className="mr-2" />
  Đã tạo
</Button>
```

---

### 6.2 Validation States

**Field-level validation (inline):**
```
┌─────────────────────────────────┐
│ Mã Lớp *                        │
│ [cs101-a-2025]                  │  ← Invalid: not uppercase
│ ❌ Mã lớp phải viết hoa          │
└─────────────────────────────────┘
```

**Form-level validation (top banner):**
```
┌──────────────────────────────────────────────────────────┐
│ ❌ Vui lòng sửa các lỗi sau:                             │
│ • Mã lớp phải viết hoa                                   │
│ • Ngày bắt đầu phải là ngày trong tương lai             │
│ • Lịch học phải chọn ít nhất 1 ngày                     │
│                                               [✕ Đóng]  │
└──────────────────────────────────────────────────────────┘
```

---

### 6.3 Success States

**Toast Notifications:**
```
┌──────────────────────────────────┐
│ ✅ Lớp CS101-A-2025 đã được tạo  │  ← Toast (top-right)
│    với 36 buổi học               │
└──────────────────────────────────┘
```

**In-page Success Messages:**
```
┌──────────────────────────────────────────────────────────┐
│ 🎉 Hoàn thành!                                           │
│                                                          │
│ Lớp học CS101-A-2025 đã được gửi duyệt thành công.     │
│ Bạn sẽ nhận được thông báo khi có kết quả.              │
│                                                          │
│              [Xem lớp học]  [Tạo lớp mới]               │
└──────────────────────────────────────────────────────────┘
```

**Component Mapping:**
- **Toast**: Use `sonner` (already installed)
- **Success dialog**: `<Dialog>` component with success icon

---

### 6.4 Error States

**400 Validation Error:**
```
┌──────────────────────────────────────────────────────────┐
│ ❌ Lỗi Xác Thực                                          │
│                                                          │
│ • Mã lớp: không được để trống                           │
│ • Sức chứa: phải lớn hơn 0                              │
│ • Lịch học: phải chọn ít nhất 1 ngày                    │
│                                                          │
│                                  [Đóng]                  │
└──────────────────────────────────────────────────────────┘
```

**400 Business Logic Error:**
```
┌──────────────────────────────────────────────────────────┐
│ ⚠️ Không thể gán tài nguyên                              │
│                                                          │
│ Lớp học phải ở trạng thái DRAFT để gán tài nguyên.      │
│ Trạng thái hiện tại: SCHEDULED                          │
│                                                          │
│                                  [Đóng]                  │
└──────────────────────────────────────────────────────────┘
```

**404 Not Found:**
```
┌──────────────────────────────────────────────────────────┐
│ ❌ Không Tìm Thấy                                        │
│                                                          │
│ Lớp học với ID 999 không tồn tại.                       │
│                                                          │
│                [Quay lại danh sách]                      │
└──────────────────────────────────────────────────────────┘
```

**Network Error:**
```
┌──────────────────────────────────────────────────────────┐
│ 🔌 Lỗi Kết Nối                                           │
│                                                          │
│ Không thể kết nối đến máy chủ. Vui lòng kiểm tra        │
│ kết nối internet và thử lại.                            │
│                                                          │
│                        [Thử lại]                         │
└──────────────────────────────────────────────────────────┘
```

**Component Mapping:**
- **Error dialog**: `<Dialog>` or `<Alert>` component
- **Field errors**: Inline text với `<AlertCircle>` icon
- **Toast errors**: `toast.error()` from sonner

---

## 7. Error Handling UX

### 7.1 Validation Error Flow

**Frontend Validation (Instant):**
```
User types → onBlur → validate → show inline error
```

**Backend Validation (After submit):**
```
User clicks submit
  → Frontend validates
  → If valid: POST to backend
  → Backend validates
  → If 400: Parse error.data object
  → Display field-level errors inline
  → Focus first error field
```

**Example Implementation:**
```typescript
// Field-level error from backend
{
  "success": false,
  "message": "Validation failed",
  "data": {
    "code": "must not be blank",
    "maxCapacity": "must be greater than 0"
  }
}

// Frontend displays:
// - "code" field: ❌ must not be blank
// - "maxCapacity" field: ❌ must be greater than 0
// - Focus on "code" field
```

---

### 7.2 Conflict Handling UX

**STEP 4 Resource Conflicts:**

1. **Detection**: Backend returns `conflictCount > 0`
2. **Display**: Show conflict dialog với table
3. **Resolution**: User selects alternative resources
4. **Retry**: Frontend calls individual session APIs
5. **Success**: Close dialog, continue workflow

**STEP 5B Teacher Partial Assignment:**

1. **Detection**: Backend returns `needsSubstitute: true`
2. **Display**: Show dialog "24 buổi chưa có giáo viên"
3. **Options**:
   - [Gán giáo viên khác]: Navigate back to STEP 5A với filter remaining sessions
   - [Tiếp tục]: Continue to STEP 6 (validation will fail)

---

### 7.3 Network Error Handling

**Retry Strategy:**
```
Request → Network Error
  ↓
Show error toast with [Thử lại] button
  ↓
User clicks [Thử lại]
  ↓
Retry same request (max 3 times)
  ↓
If still fails: Show persistent error dialog
```

**Offline Detection:**
```tsx
// Use navigator.onLine
if (!navigator.onLine) {
  toast.error("Bạn đang offline. Vui lòng kiểm tra kết nối.")
}
```

---

## 8. Responsive Design

### 8.1 Mobile Layout (< 768px)

**Changes from Desktop:**

1. **Progress Indicator**: Horizontal scroll hoặc vertical list
2. **Form Layout**: Single column (không 2 columns)
3. **Tables**: Horizontal scroll hoặc card-based view
4. **Dialogs**: Full screen modals
5. **Footer buttons**: Stack vertically hoặc smaller padding

**Mobile-specific Optimizations:**
- Larger touch targets (min 44×44px)
- Sticky footer for action buttons
- Collapsible sections to reduce scroll
- Auto-scroll to validation errors

---

### 8.2 Tablet Layout (768px - 1024px)

**Hybrid Approach:**
- 2-column form layout for STEP 1
- Tables with horizontal scroll if needed
- Sidebar navigation (if applicable)

---

### 8.3 Desktop Layout (> 1024px)

**Full Layout:**
- 2-column forms
- Full-width tables with sorting/filtering
- Larger modals (max-width: 800px)
- Sidebar + main content area

---

## 9. Accessibility

### 9.1 Keyboard Navigation

**Tab Order:**
```
Progress indicator → Form fields (top to bottom) → Footer buttons
```

**Shortcuts:**
- `Enter`: Submit current step
- `Esc`: Close dialogs/modals
- `←/→ Arrow keys`: Navigate steps (in progress indicator)
- `Tab/Shift+Tab`: Navigate form fields

---

### 9.2 Screen Reader Support

**ARIA Labels:**
```tsx
<button aria-label="Quay lại bước trước">
  <ChevronLeft />
</button>

<div role="alert" aria-live="assertive">
  {validationError}
</div>

<div role="status" aria-live="polite">
  {successMessage}
</div>
```

**Focus Management:**
- Auto-focus first field on step load
- Focus first error field after validation
- Return focus to trigger after closing dialog

---

### 9.3 Color Contrast

**WCAG AA Compliance:**
- Text: 4.5:1 contrast ratio minimum
- Large text (18pt+): 3:1 contrast ratio
- Interactive elements: Clear focus indicators

**Color-blind Friendly:**
- Don't rely on color alone (use icons + text)
- Green ✅ + "Thành công"
- Red ❌ + "Lỗi"
- Yellow ⚠️ + "Cảnh báo"

---

## 10. Implementation Notes

### 10.1 State Management

**Recommendation: React Hook Form + Zod**

```typescript
// STEP 1 Form Schema
import { z } from 'zod'

const createClassSchema = z.object({
  branchId: z.number().positive(),
  courseId: z.number().positive(),
  code: z.string()
    .min(1, "Mã lớp không được để trống")
    .max(50, "Mã lớp tối đa 50 ký tự")
    .regex(/^[A-Z0-9-]+$/, "Mã lớp chỉ chứa chữ hoa, số và dấu gạch ngang"),
  name: z.string()
    .min(1, "Tên lớp không được để trống")
    .max(255, "Tên lớp tối đa 255 ký tự"),
  modality: z.enum(['ONLINE', 'OFFLINE', 'HYBRID']),
  startDate: z.string()
    .refine(date => new Date(date) > new Date(), "Ngày bắt đầu phải là ngày trong tương lai"),
  scheduleDays: z.array(z.number().min(0).max(6))
    .min(1, "Phải chọn ít nhất 1 ngày")
    .max(7, "Tối đa 7 ngày"),
  maxCapacity: z.number()
    .min(1, "Sức chứa phải ít nhất 1")
    .max(1000, "Sức chứa tối đa 1000")
})

type CreateClassForm = z.infer<typeof createClassSchema>
```

---

### 10.2 API Integration (RTK Query)

**Create API Service:**

```typescript
// src/store/services/classCreationApi.ts
import { createApi } from '@reduxjs/toolkit/query/react'
import { baseQueryWithReauth } from './authApi'

export const classCreationApi = createApi({
  reducerPath: 'classCreationApi',
  baseQuery: baseQueryWithReauth,
  endpoints: (builder) => ({
    // STEP 1: Create Class
    createClass: builder.mutation({
      query: (data) => ({
        url: '/classes',
        method: 'POST',
        body: data
      })
    }),

    // STEP 3: Assign Time Slots
    assignTimeSlots: builder.mutation({
      query: ({ classId, assignments }) => ({
        url: `/classes/${classId}/time-slots`,
        method: 'POST',
        body: { assignments }
      })
    }),

    // STEP 4: Assign Resources
    assignResources: builder.mutation({
      query: ({ classId, pattern }) => ({
        url: `/classes/${classId}/resources`,
        method: 'POST',
        body: { pattern }
      })
    }),

    // STEP 5A: Get Available Teachers
    getAvailableTeachers: builder.query({
      query: (classId) => `/classes/${classId}/available-teachers`
    }),

    // STEP 5B: Assign Teacher
    assignTeacher: builder.mutation({
      query: ({ classId, teacherId, sessionIds }) => ({
        url: `/classes/${classId}/teachers`,
        method: 'POST',
        body: { teacherId, sessionIds }
      })
    }),

    // STEP 6: Validate Class
    validateClass: builder.mutation({
      query: (classId) => ({
        url: `/classes/${classId}/validate`,
        method: 'POST'
      })
    }),

    // STEP 7: Submit for Approval
    submitClass: builder.mutation({
      query: (classId) => ({
        url: `/classes/${classId}/submit`,
        method: 'POST'
      })
    })
  })
})

export const {
  useCreateClassMutation,
  useAssignTimeSlotsMutation,
  useAssignResourcesMutation,
  useGetAvailableTeachersQuery,
  useAssignTeacherMutation,
  useValidateClassMutation,
  useSubmitClassMutation
} = classCreationApi
```

---

### 10.3 Workflow State Management

**Use URL params + localStorage for draft persistence:**

```typescript
// URL: /academic/classes/create?step=3&classId=123

// Store draft in localStorage
const saveDraft = (step: number, data: any) => {
  localStorage.setItem(`class-draft-${classId}`, JSON.stringify({
    step,
    data,
    timestamp: Date.now()
  }))
}

// Load draft on mount
const loadDraft = () => {
  const draft = localStorage.getItem(`class-draft-${classId}`)
  if (draft) {
    const { step, data, timestamp } = JSON.parse(draft)
    // Check if draft is < 24 hours old
    if (Date.now() - timestamp < 24 * 60 * 60 * 1000) {
      return { step, data }
    }
  }
  return null
}
```

---

### 10.4 Component Structure

```
src/app/academic/classes/create/
├── page.tsx                      # Main wizard container
├── components/
│   ├── CreateClassWizard.tsx     # Wizard wrapper with progress indicator
│   ├── Step1BasicInfo.tsx        # STEP 1 form
│   ├── Step2ReviewSessions.tsx   # STEP 2 (optional)
│   ├── Step3TimeSlots.tsx        # STEP 3 pattern assignment
│   ├── Step4Resources.tsx        # STEP 4 pattern assignment
│   ├── Step5ATeachers.tsx        # STEP 5A teacher list
│   ├── Step5BAssignTeacher.tsx   # STEP 5B confirmation
│   ├── Step6Validate.tsx         # STEP 6 validation summary
│   ├── Step7Submit.tsx           # STEP 7 final review
│   ├── ProgressIndicator.tsx     # Progress bar/stepper
│   ├── ConflictDialog.tsx        # Resource conflict resolution
│   ├── WizardFooter.tsx          # Back/Next buttons
│   └── SuccessDialog.tsx         # Final success message
└── hooks/
    ├── useWizardNavigation.ts    # Step navigation logic
    └── useDraftPersistence.ts    # Auto-save draft
```

---

### 10.5 Performance Optimizations

**Lazy Loading:**
```tsx
const Step1 = lazy(() => import('./components/Step1BasicInfo'))
const Step2 = lazy(() => import('./components/Step2ReviewSessions'))
// ... other steps

<Suspense fallback={<Skeleton />}>
  {currentStep === 1 && <Step1 />}
  {currentStep === 2 && <Step2 />}
  {/* ... */}
</Suspense>
```

**Debounce Validation:**
```tsx
const debouncedValidate = useMemo(
  () => debounce((value) => validateField(value), 300),
  []
)
```

**Optimistic Updates:**
```tsx
// Don't wait for API response to navigate to next step
await createClass(data)
navigateToStep(3)  // Immediately navigate
```

---

## 11. Figma Mockup Structure (If Needed)

**Recommended Figma Flow:**

1. **Cover Page**: Feature overview, user flow diagram
2. **Design System**: Colors, typography, spacing, components
3. **Desktop Wireframes**: 7 steps × desktop layout
4. **Mobile Wireframes**: 7 steps × mobile layout
5. **Component Library**: All reusable components
6. **Interactive Prototype**: Clickable flow from STEP 1 → 7

---

## 12. Success Metrics

**UX Metrics to Track:**

- **Task Completion Rate**: % users who complete all 7 steps
- **Time on Task**: Average time from STEP 1 → 7
- **Error Rate**: % users who encounter validation errors
- **Conflict Resolution Rate**: % users who successfully resolve resource conflicts
- **Drop-off Rate**: Which step has highest abandonment
- **User Satisfaction**: Post-task survey (1-5 stars)

**Technical Metrics:**

- **API Response Time**: Track `processingTimeMs` from responses
- **Frontend Rendering Time**: Measure step load time
- **Error Rate**: Track 4xx/5xx errors
- **Retry Rate**: How often users retry failed requests

---

## 13. Appendix: Vietnamese Day Names Helper

```typescript
// src/utils/dayNames.ts
export const DAY_NAMES_VI = {
  0: 'Chủ Nhật',
  1: 'Thứ 2',
  2: 'Thứ 3',
  3: 'Thứ 4',
  4: 'Thứ 5',
  5: 'Thứ 6',
  6: 'Thứ 7'
} as const

export const DAY_NAMES_SHORT_VI = {
  0: 'CN',
  1: 'T2',
  2: 'T3',
  3: 'T4',
  4: 'T5',
  5: 'T6',
  6: 'T7'
} as const

export const getDayName = (dow: number): string => {
  return DAY_NAMES_VI[dow as keyof typeof DAY_NAMES_VI]
}

export const getDayNameShort = (dow: number): string => {
  return DAY_NAMES_SHORT_VI[dow as keyof typeof DAY_NAMES_SHORT_VI]
}
```

---

## 14. Final Checklist

### Design Completeness
- [x] Progress indicator design
- [x] All 7 steps wireframes
- [x] Conflict resolution dialog
- [x] Error states for all scenarios
- [x] Success states
- [x] Loading states
- [x] Mobile responsive layout
- [x] Accessibility considerations

### Component Mapping
- [x] All shadcn/ui components identified
- [x] Custom components specified
- [x] Props interfaces defined
- [x] State management approach

### Implementation Ready
- [x] API integration plan (RTK Query)
- [x] Form validation schemas (Zod)
- [x] Vietnamese translations
- [x] Error handling strategy
- [x] Performance optimizations

---

**End of UX Design Document**

**Next Steps:**
1. Review with team
2. Create Figma mockups (if needed)
3. Implement components
4. Write unit tests
5. Integration testing
6. User acceptance testing
